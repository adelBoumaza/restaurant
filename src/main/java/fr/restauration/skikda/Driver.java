package fr.restauration.skikda;

import java.util.Arrays;
import java.util.List;
import java.util.Optional;
import java.util.stream.Collectors;
import java.util.stream.IntStream;

import fr.restauration.skikda.dto.CommandeDto;
import fr.restauration.skikda.entities.Commande;
import fr.restauration.skikda.entities.Produit;

public class Driver {

	public static void main(String[] args) {
		/*CommandeDto commandeDto = new CommandeDto();
		//commandeDto.setListIdProduit(Arrays.asList(1,2,3,4,5));
		List<Produit> produits = IntStream.range(1, commandeDto.getListIdProduit().size())
				.mapToObj(id -> new Produit(id)).collect(Collectors.toList());
		Commande commande = new Commande();
		commande.setProduits(produits);
		System.out.println(commande);*/
			
	}
	
	
	
}
